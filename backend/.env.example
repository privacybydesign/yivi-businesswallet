# PostgreSQL connection string. The backend defaults to a local Postgres on
# localhost:5432, so this only needs setting to point at a different instance.
# In Docker Compose it is provided automatically by the `backend` service.
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/yivi_business_wallet?sslmode=disable
